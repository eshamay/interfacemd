#ifndef FREQTEST_H_
#define FREQTEST_H_

#include <stdio.h>
#include <stdlib.h>
#include "../utility.h"
#include "../watersfg.h"
#include "../matrixr.h"

#define PRMTOP	"prmtop"
#define MDCRD	"mdcrd"
#define FORCE	"mdvel"

#define OUTPUT_FREQ		10				// how often the output file will be written (# of timesteps/10)

//#define MOLS			1000000
#define TIMESTEPS		100000			// # of timesteps to process through

//#define SPS
#define SSP

#ifdef SSP
#define OUTPUTFILE		"chi.SSP.dat"	// name of the output file for the final spectra
#endif

#ifdef SPS
#define OUTPUTFILE		"chi.SPS.dat"
#endif

// set the interface correctly - different simulation = different location!!
#define INTERFACE		28.0			// the location of the interface (the top one, only, for now)

#endif
